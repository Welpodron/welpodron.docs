// import { Alert } from '@/components/alert/Alert';
import { Sandbox } from '@/components/sandbox/Sandbox';

// export type MdxAlertPropsType = {
//   children: React.ReactNode;
// };

export const MdxSandbox = ({ ...props }) => {
  return <Sandbox {...props} />;
};
